# RIN GARDEN SYSTEM

このファイルは Claude Code（および本プロジェクトで作業する全ての人・AI）に向けた行動規範です。
コード変更を行う前に必ず読んでください。

## システム名称

**RIN GARDEN SYSTEM**（凜ガーデン・システム）

JRA中央競馬 / 地方競馬（NAR）/ BOAT RACE / 競輪 の4競技を対象とした、
公営競技の分析・検証システムです。

## 基本思想

> 結果を知った後から予想を書き換えてはならない。

このシステムの目的は「的中数を増やすこと」ではありません。最重要目標は以下です。

- 長期ROI
- 純利益
- 高払戻の再現性
- 市場価格に対する優位性（VALUE）
- **予想時点の情報を改ざんせず残すこと**
- 全レース検証による選別精度の改善

予想精度そのものよりも、**予想時点のデータ完全性**を重要視します。
本システムは実購入・自動投票システムではありません。追い上げ・マーチン・負け取り目的の増額は禁止です。

## 最重要禁止事項

1. 結果後のPRE改変禁止
2. FINAL-LOCK後の買い目改変禁止（revision以外での上書き禁止）
3. Race IDが一致しないデータの混入禁止
4. 日付違いの結果流用禁止
5. 会場違いの結果流用禁止
6. R番号だけでレースを識別しない
7. 検索結果だけを根拠にRace Identityを確定しない
8. 架空データを補完しない
9. 取得不能情報を推測で埋めない
10. PREが存在しないレースを結果後に予想したことにしない

これらは「プロンプト上のお願い」ではなく、`rin_garden/core/locks.py` および
`rin_garden/audit/` のコードとテスト (`tests/`) によって**システムとして強制**されます。
プロンプトや記憶に安全性を依存させないでください。

## 判断原則

- 事実（fact）、推測（inference）、シナリオ（scenario）を分離して扱う。
- データが不足している場合は `DATA_INCOMPLETE` または `DATA_CONFIDENCE` を明示する。
- 取得できない値は None / 未設定のままとし、それらしい数値で埋めない。

## コア概念

### Race Master（`rin_garden/core/race_master.py`）

全競技共通のレース同一性管理台帳。`sport / date / venue / race_number` の組み合わせだけを
主キーにせず、`race_id`（決定的に生成される識別子）と `event_id` / `canonical_source` を
中心にレース同一性を管理します。詳細は `rin_garden/core/identity.py`。

### PRE-FIX（`rin_garden/core/pre_fix.py`）

結果を知らない時点で作成した予想の原本。**発走時刻（scheduled_start）より前**にのみ作成でき、
一度保存すると通常の処理では変更できません（immutable）。

### FINAL-LOCK（`rin_garden/core/final_lock.py`）

直前情報取得後の最終判断（印・評価・買い目・金額・BUY/WAIT/SKIP）。作成後の変更は
上書きではなく **revision（差分＋監査ログ）** として記録されます。発走後のrevisionは禁止です。

### NO POST-HOC（`rin_garden/core/locks.py`, `rin_garden/audit/posthoc.py`)

結果取得後に PRE / FINAL を新規作成したり、有利な方向へ書き換えたりすることを防ぐ検証機構。
`scheduled_start` と操作時刻 `now` を比較し、違反時は例外 `PostHocBlockedError` を送出し、
ログに `POST_HOC_BLOCKED` として記録した上で処理を拒否します。

## GARDEN-6（`agents/*.md`, `rin_garden/analysis/garden6.py`）

6人の独立した分析担当が、それぞれ独立に分析し、最後に凜が統合します。全員が同じ結論に
寄せてはいけません。詳細な担当領域・口調は `agents/` 配下の各Markdownを参照してください。

| 名前 | 担当 |
|---|---|
| 凜 (Rin) | 総合責任者・最終決裁（期待値・資金配分・BUY/WAIT/SKIP） |
| 美羽香 (Miuka) | データ担当（過去成績・数値・統計・市場データ） |
| 花奈 (Kana) | 現場担当（当日状態・気配・直前変化） |
| 英梨紗 (Erisa) | 監査担当（過大評価・矛盾・リスク・買い目のガミ） |
| 黒音 (Kurone) | 穴・シナリオ担当（CHAOS・人気崩壊・高配当ルート） |
| 瑠那 (Luna) | 検証担当（バックテスト・ROI・Calibration） |

キャラクター会話を優先して分析量を削ってはいけません。

## レース評価軸

- ランク: `S / A / B / C / D / E`（主な買い候補は S/A/B。C/D/Eも検証用に保存する）
- CHAOS: `0〜10`（ランクとは独立した別軸）
- VALUE: `estimated_probability / fair_odds / market_odds / expected_value` を用いて
  人気順ではなく「推定確率と市場オッズの差」を評価する。

## データ取得方針

- 公式API・正規データソースを優先する。
- Webサイト構造へ強く依存する脆弱なスクレイピングは最後の手段。
- 利用規約・robots制限を無視する処理は作らない。
- 取得できない情報は `DATA_INCOMPLETE` として明示し、推測で埋めない。

## Secrets

APIキー・Google認証情報などはソースコードに直書きせず、環境変数（`.env`、Gitにコミットしない）
で管理します。`.env.example` にキー名のみを記載します。

## 開発時の注意

- 巨大な1ファイルに機能を詰め込まない。小さな関数・小さなモジュールを優先する。
- 設定とコードを分離する（`config/*.yaml`）。
- 同じコマンドを2回実行してもデータが壊れない設計（冪等性）を優先する。
- 既存ファイルを勝手に削除・上書き・破壊的変更しない。必要ならバックアップを作る。
